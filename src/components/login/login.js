import React from "React"

export default class Login extends React.Component {

    constructor(props) {
        super(props)
    }

    render() {
        return (
            <div style={{marginTop: "100px"}}>
                <p>Sono la pagina di login</p>
                <p>Sono la pagina di login</p>
                <p>Sono la pagina di login</p>
            </div>
        )
    }
}